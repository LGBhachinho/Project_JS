for (let pas = 0; pas <= 30; pas += 3) {
    console.log(pas )
    // if (pas % 3 === 0) {    //  Si le pas est un multiple de 3  si la boucle for en pas de 1
    //     console.log(pas );
    // } 
}